name: Vinted Scanner Michele

on:
  workflow_dispatch:
  schedule:
    - cron: "7,22,37,52 * * * *"

jobs:
  scan:
    uses: ./.github/workflows/_vinted-scanner-template.yml
    secrets:
      vinted_config: ${{ secrets.VINTED_CONFIG_MICHELE }}
