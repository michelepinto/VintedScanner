name: Vinted Scanner Sara

on:
  workflow_dispatch:
  schedule:
    - cron: "12,27,42,57 * * * *"

permissions:
  contents: write

jobs:
  scan:
    uses: ./.github/workflows/_vinted-scanner-template.yml
    secrets:
      vinted_config: ${{ secrets.VINTED_CONFIG_SARA }}
